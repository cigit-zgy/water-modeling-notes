---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
---

<Layout
  title="Diagram system preview"
  description="Mermaid and PlantUML diagram templates for the academic blog."
>
  <Header />
  <main id="main-content" class="content-layout py-12 sm:py-16">
    <header class="mb-12">
      <p class="text-link text-xs font-semibold tracking-[0.12em] uppercase">
        Diagram system
      </p>
      <h1 class="mt-3 text-4xl font-semibold tracking-[-0.04em] sm:text-5xl">
        Mermaid + PlantUML
      </h1>
      <p class="text-muted-foreground mt-4 max-w-2xl text-lg leading-relaxed">
        两套图形使用同一套 Apple/Liquid Glass 深色语义色，并统一限制在正文阅读宽度内。发布页只加载预渲染的本地 SVG。
      </p>
    </header>

    <article class="app-prose">
      <section>
        <h2>Mermaid：科研工作流</h2>
        <p>
          Mermaid 用于普通 workflow、架构概览和有向流程。模板缩小 node/rank spacing，并优先通过分组方向压缩纵向高度。
        </p>
        <figure>
          <img
            src="/figures/sci-manuscript-lifecycle-mermaid.svg"
            alt="使用 Mermaid 绘制的 sci-manuscript-skill 稿件生命周期流程图"
            width="760"
            height="475"
            loading="eager"
            decoding="async"
          />
          <figcaption>
            <strong>Figure 1.</strong> Mermaid 模板示例：稿件生命周期。显示宽度约 760 px，节点和层级间距保持紧凑。
          </figcaption>
        </figure>
      </section>

      <section>
        <h2>PlantUML：人—Agent—工具链交互</h2>
        <p>
          PlantUML 用于 sequence、component 和其他 UML 型图形。参与者数量受控，消息文本保持短句，避免横向无限扩张。
        </p>
        <figure>
          <img
            src="/figures/sci-manuscript-operation.svg"
            alt="使用 PlantUML 绘制的 Supervisor、LLM Agent、Skill、Python package 和 LaTeX toolchain 交互序列图"
            width="691"
            height="484"
            loading="eager"
            decoding="async"
          />
          <figcaption>
            <strong>Figure 2.</strong> PlantUML 模板示例：生命周期操作的授权与执行边界。
          </figcaption>
        </figure>
      </section>
    </article>
  </main>
  <Footer />
</Layout>
